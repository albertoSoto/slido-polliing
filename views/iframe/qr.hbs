<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>QR Code</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    
    body { 
      margin: 0; 
      padding: 40px; 
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      text-align: center;
      background: white;
      height: 100vh;
      box-sizing: border-box;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
    }

    .qr-container {
      background: white;
      border-radius: 20px;
      padding: 30px;
      box-shadow: 0 4px 20px rgba(0,0,0,0.1);
    }
    
    .qr-code { 
      max-width: 300px; 
      height: auto; 
      border-radius: 10px;
    }
    
    .url { 
      margin-top: 20px; 
      font-size: 18px; 
      color: #666;
      word-break: break-all;
    }
    
    .qr-title {
      font-size: 28px;
      font-weight: bold;
      color: #333;
      margin-bottom: 20px;
    }

    .waiting {
      font-size: 20px;
      color: #999;
    }
  </style>
</head>
<body>
  {{#if hasPoll}}
    <div class="qr-container">
      <div class="qr-title">📱 Scan to Vote</div>
      {{#if qrCode}}
        <img src="{{qrCode}}" alt="QR Code" class="qr-code" />
      {{/if}}
      <div class="url">{{voteUrl}}</div>
    </div>
  {{else}}
    <div class="waiting">Waiting for poll to start...</div>
  {{/if}}
</body>
</html>