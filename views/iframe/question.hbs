<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Poll Question</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    
    body { 
      margin: 0; 
      padding: 40px; 
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      text-align: center;
      background: white;
      height: 100vh;
      box-sizing: border-box;
      display: flex;
      flex-direction: column;
      justify-content: center;
    }

    .question {
      font-size: 36px;
      font-weight: bold;
      color: #333;
      margin-bottom: 40px;
      line-height: 1.2;
    }

    .options {
      display: flex;
      flex-direction: column;
      gap: 20px;
      max-width: 600px;
      margin: 0 auto;
    }

    .option {
      background: #f8f9fa;
      border: 2px solid #dee2e6;
      border-radius: 12px;
      padding: 20px;
      font-size: 20px;
      color: #495057;
    }
  </style>
</head>
<body>
  <div class="question">{{question}}</div>
  <div class="options">
    {{#each options}}
      <div class="option">{{letter}}) {{text}}</div>
    {{/each}}
  </div>
</body>
</html>